    public final java.lang.Object runUntilValue() throws java.lang.Throwable
    {
        java.lang.Throwable $r4;
        gnu.mapping.CallContext r0;
        gnu.mapping.ValueStack $r3;
        int $i0, $i1, $i2;
        java.lang.Object $r1;
        gnu.lists.Consumer $r2;

        r0 := @this: gnu.mapping.CallContext;

        $r2 = r0.<gnu.mapping.CallContext: gnu.lists.Consumer consumer>;

        $r3 = r0.<gnu.mapping.CallContext: gnu.mapping.ValueStack vstack>;

        r0.<gnu.mapping.CallContext: gnu.lists.Consumer consumer> = $r3;

        $i0 = $r3.<gnu.mapping.ValueStack: int gapStart>;

        $i1 = $r3.<gnu.mapping.ValueStack: int oindex>;

     label1:
        virtualinvoke r0.<gnu.mapping.CallContext: void runUntilDone()>();

        $i2 = $r3.<gnu.mapping.ValueStack: int gapStart>;

        $r1 = staticinvoke <gnu.mapping.Values: java.lang.Object make(gnu.lists.TreeList,int,int)>($r3, $i0, $i2);

     label2:
        r0.<gnu.mapping.CallContext: gnu.lists.Consumer consumer> = $r2;

        $r3.<gnu.mapping.ValueStack: int gapStart> = $i0;

        $r3.<gnu.mapping.ValueStack: int oindex> = $i1;

        return $r1;

     label3:
        $r4 := @caughtexception;

        r0.<gnu.mapping.CallContext: gnu.lists.Consumer consumer> = $r2;

        $r3.<gnu.mapping.ValueStack: int gapStart> = $i0;

        $r3.<gnu.mapping.ValueStack: int oindex> = $i1;

        throw $r4;

        catch java.lang.Throwable from label1 to label2 with label3;
    }
