    private void init(com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams)
    {
        com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams $r1, $r2;
        com.biznessapps.utils.google.caching.ImageCache r0;
        int $i0;
        boolean $z0;
        com.biznessapps.utils.google.caching.ImageCache$1 $r3;

        r0 := @this: com.biznessapps.utils.google.caching.ImageCache;

        $r1 := @parameter0: com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams;

        r0.<com.biznessapps.utils.google.caching.ImageCache: com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams mCacheParams> = $r1;

        $r2 = r0.<com.biznessapps.utils.google.caching.ImageCache: com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams mCacheParams>;

        $z0 = $r2.<com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams: boolean memoryCacheEnabled>;

        if $z0 == 0 goto label1;

        $r3 = new com.biznessapps.utils.google.caching.ImageCache$1;

        $r2 = r0.<com.biznessapps.utils.google.caching.ImageCache: com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams mCacheParams>;

        $i0 = $r2.<com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams: int memCacheSize>;

        specialinvoke $r3.<com.biznessapps.utils.google.caching.ImageCache$1: void <init>(com.biznessapps.utils.google.caching.ImageCache,int)>(r0, $i0);

        r0.<com.biznessapps.utils.google.caching.ImageCache: android.support.v4.util.LruCache mMemoryCache> = $r3;

     label1:
        $z0 = $r1.<com.biznessapps.utils.google.caching.ImageCache$ImageCacheParams: boolean initDiskCacheOnCreate>;

        if $z0 == 0 goto label2;

        virtualinvoke r0.<com.biznessapps.utils.google.caching.ImageCache: void initDiskCache()>();

     label2:
        return;
    }
