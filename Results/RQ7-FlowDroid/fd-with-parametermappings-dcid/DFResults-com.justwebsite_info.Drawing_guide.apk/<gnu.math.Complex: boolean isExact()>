    public boolean isExact()
    {
        gnu.math.RealNum $r1;
        gnu.math.Complex r0;
        boolean $z0;

        r0 := @this: gnu.math.Complex;

        $r1 = virtualinvoke r0.<gnu.math.Complex: gnu.math.RealNum re()>();

        $z0 = virtualinvoke $r1.<gnu.math.RealNum: boolean isExact()>();

        if $z0 == 0 goto label1;

        $r1 = virtualinvoke r0.<gnu.math.Complex: gnu.math.RealNum im()>();

        $z0 = virtualinvoke $r1.<gnu.math.RealNum: boolean isExact()>();

        if $z0 == 0 goto label1;

        return 1;

     label1:
        return 0;
    }
